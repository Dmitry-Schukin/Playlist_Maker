<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Активное состояние (включен) -->
    <item android:color="@color/light_blue_background_color" android:state_checked="true" />
    <!-- Неактивное состояние (выключен) -->
    <item android:color="@color/light_gray_background_color" android:state_checked="false" />
</selector>